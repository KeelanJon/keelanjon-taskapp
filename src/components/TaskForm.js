import React from "react";
import styled from "styled-components";

function TaskForm() {
  return <FormContainer>This is a form</FormContainer>;
}

export default TaskForm;

const FormContainer = styled.div``;
